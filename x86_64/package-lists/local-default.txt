# x86_64 默认集成的本地包
# 空行和 # 开头的注释会被 x86_64/build24.sh 忽略。
# daed / luci-app-daede 来自 kenzok8/openwrt-daede 预编译 x86_64 feed，
# vmlinux-btf 用于补齐 dae/daed eBPF 运行所需的内核 BTF。

daed
luci-app-daede
vmlinux-btf
